[package]
name = "pathland-layout"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Pathland layout engine: VStack/HStack/Text layout emitting 16-byte opcodes (zero-alloc during layout)"
repository = "https://github.com/michaelkrog/Pathland"
categories = ["graphics"]
keywords = ["ui", "layout", "opcode", "wasm"]

[dependencies]
pathland-opcode = { path = "../pathland-opcode" }

[dev-dependencies]
