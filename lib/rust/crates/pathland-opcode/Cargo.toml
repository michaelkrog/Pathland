[package]
name = "pathland-opcode"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Pathland 16-byte opcode engine: fixed-size opcodes, ring buffer, arena, linear-memory layout"
repository = "https://github.com/michaelkrog/Pathland"
categories = ["encoding", "wasm", "graphics"]
keywords = ["ui", "protocol", "opcode", "wasm", "ring-buffer"]

[dependencies]
