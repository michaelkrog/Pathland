[package]
name = "pathland-engine"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Pathland guest engine: retained view tree -> declarative TREE/STYLE opcodes with diff-based (reactive) emission"
repository = "https://github.com/michaelkrog/Pathland"
categories = ["graphics"]
keywords = ["ui", "opcode", "wasm", "reactive", "diff"]

[dependencies]
pathland-opcode = { path = "../pathland-opcode" }
