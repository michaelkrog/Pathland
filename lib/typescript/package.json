{
  "name": "pathland-typescript",
  "version": "1.0.0",
  "description": "Pathland TypeScript libraries - monorepo root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "cd packages/protocol && npm run build && cd ../../packages/transport && npm run build && cd ../../packages/view && npm run build && cd ../../packages/platform-browser && npm run build && cd ../../packages/renderer-dom && npm run build",
    "test": "npm test --workspaces --if-present",
    "build:view": "cd packages/view && npm run build",
    "watch:view": "cd packages/view && npm run watch"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
